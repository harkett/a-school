.env
.env.*
!.env.example
.venv/
__pycache__/
*.pyc
outputs/
data/
# Frontend
node_modules/
frontend/dist/
frontend/node_modules/
# Runtime
backend.pid
frontend.pid
.run_pids
# Fichiers personnels hors projet
MesRessources/
parse_markdown.py
MesMD/EMAIL_PILOTES.md
MesMD/FONCTIONNALITES_ASCHOOL.md
MesMD/MATRICE_ACTIVITES_ASCHOOL.md
# Audio de test (samples Deepgram & autres) — binaires, jamais commit
MesMD/**/*.wav
# Sauvegardes locales .bak (filets avant edition/suppression) — jamais commitees
*.bak-*
# Scripts ephemeres (regle « script ephemere » : jamais commites)
_tmp_*.py
# Documents sources a analyser (PDF lourds — gardes en local, hors depot)
MesMD/DOCS_A_ANALYSER/*.pdf
# Sauvegardes navigateur (« enregistrer sous » — derive, hors depot)
MesMD/*_files/
MesMD/*.html
# Procedures imprimees perso (hors depot)
MesAdmin/
# Carte de la base : sortie regeneree a la demande (on la reconstruit, jamais versionnee)
outils_bdd/carte_base/carte_base.html
# Referentiels : PDF/extractions generes et remanies pendant les tests (artefacts, hors depot)
REFERENTIELS/
# Apercu HTML genere depuis un .md (« enregistrer sous » — artefact, hors depot)
Scripts/bilan.html
# Config editeur perso VS Code (workspace local, hors depot)
A-SCHOOL.code-workspace
# Docker local (base + caches, 4,4 Go) — donnees locales du poste, JAMAIS en depot ni sur le serveur
docker/
