# D14 — Commit accumulé session 18/05/2026

**Statut :** 📦 À committer — étape 0 avant tout autre travail
**Date ouverture :** 2026-05-18

---

## Pourquoi

Plusieurs livrables + travaux de restructuration s'accumulent non commités. Hygiène : à committer en groupes logiques avant d'attaquer la suite (D09 runs vocaux, etc.).

---

## Commits à produire (groupes logiques)

| # | Sujet | Contenu | Source détail |
|---|---|---|---|
| 1 | `feat(consigne):` L5 Analyseur de consignes | `backend/routers/consigne.py` + `frontend/components/Consigne.jsx` + `backend/main.py` | [D01](D01.md) |
| 2 | `feat(optim):` Optimiseur inline + sidebar lean | `Optimiseur.jsx`, `Sidebar.jsx`, etc. | [D02](D02.md) |
| 3 | `feat(rag):` INFRA-RAG mutualisée + branchement /api/generate DEV | `backend/rag/` + gates dans `generate.py` + `_build_rag_prefix()` ; après commit, supprimer `rag_demo/` | [D03](D03.md) |
| 4 | `fix(generate):` Groq fallback chain 413/503 | `backend/groq_client.py` + intégration generate | [D04](D04.md) |
| 5 | `feat(ui):` errorDialog modal + matière-niveau header | `frontend/errorDialog.js` + Header + composants | [D05](D05.md) |
| 6 | `chore(branding):` finaliser migration school.afia.fr → aschool.fr (24 occurrences) | `.env.example`, `backend/auth.py`, `alerts.py`, `routers/admin.py`, `routers/fiches.py`, `ZoneResultat.jsx`, `AdminLayout.jsx`, `Footer.jsx`, `MentionsLegales.jsx`, `deploy/nginx-aschool.conf` | [D08](D08.md) |
| 7 | `docs:` restructure doc — BOUSSOLE/ aplatie + DEEPGRAM pilotage migré dans D09 + RAG/ + LEVIERS/ + ajout D10→D14 | `MesMD/BOUSSOLE/**`, `MesMD/RAG/**` (ex SPECS_TRANSVERSES), `MesMD/LEVIERS/**` (ex SPECS_LEVIERS/DETAIL_LEVIER), TRACKER, code refs | session 18/05 |

---

## Ordre recommandé

1. Commits **1 → 5** (livrables fonctionnels) — non destructifs, peuvent être faits séparément
2. Commit **6** (branding) — bien le groupe à part pour pouvoir l'annuler si problème
3. Commit **7** (docs/restructure) — gros volume de fichiers mais zéro impact code

## Méthode

- Un commit par sujet, pas un méga-commit fourre-tout
- Lire `git status` avant chaque commit pour ne pas embarquer un fichier non lié
- `push.ps1` déclenchera le déploiement VPS — donc s'assurer que les commits 1-6 sont testés en local avant push

## Effort
~30 min si tout va bien (le code est propre, la session a été disciplinée).

## Critère "fini"
- `git status` propre (zéro fichier non commité hors choix conscient)
- Section "Livré, à committer" de BOUSSOLE vidée (D01-D05 deviennent FAIT ✅ dans TRACKER)
- D14 supprimé de BOUSSOLE + ce fichier deleted

## Risques
- Pre-commit hook bloque : investiguer la cause, fixer, nouveau commit (jamais `--no-verify`)
- Mauvais groupement par erreur : `git reset` (avant push), recommencer
