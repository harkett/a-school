# D02 — Optimiseur inline + sidebar lean

**Statut :** ✅ Livré 14/05/2026 — non commité
**Spec architecturale :** aucune (refacto UX cohérent — décision UX consolidée)

## Fichiers concernés
- `frontend/src/components/SequenceForm.jsx` — bouton "Optimiser" inline + dialog confirmation + état `scoreOptim`
- `frontend/src/components/Sidebar.jsx` — refonte Mes Outils : Créer + Historique (Analyse retirée car accessible via hub `mes-outils`)
- `frontend/src/App.jsx` — retrait carte "Améliorer une séquence" du hub Accueil + bouton "Ambiguïtés" contextuel dans ZoneResultat
- `frontend/src/components/ZoneResultat.jsx` — bouton "Ambiguïtés" qui pré-remplit Ambiguites
- `frontend/src/components/Ambiguites.jsx` — props `prefillTexte` + `onPrefillUsed`
- `backend/routers/optimiseur.py` — prompt markdown structuré + `max_tokens` 6000 + `response_format: json_object` + `temperature: 0`

## Prochaine action
Commit dédié : `feat(ux): optimiseur inline + sidebar lean + accès contextuel Ambiguïtés`

## Risques
Aucun — design UX cohérent, hub `mes-outils` conserve l'accès aux outils Analyse.
