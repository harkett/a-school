# D04 — Groq fallback chain 413/503

**Statut :** ✅ Livré 15/05/2026 — non commité
**Spec architecturale :** aucune (fix infra ponctuel)

## Fichiers concernés
- `backend/groq_client.py` *(new)* — centralisation appels Groq + fallback chain sur 3 modèles alternatifs (`openai/gpt-oss-120b` → `openai/gpt-oss-20b` → `llama-3.1-8b-instant`)
- `backend/routers/ambiguites.py` — utilise `call_groq()` au lieu de `requests.post` direct
- `backend/routers/optimiseur.py` — idem
- `backend/routers/sequence.py` — idem

## Prochaine action
Commit dédié : `refactor(groq): centralisation appels + fallback chain 413/503`

## Origine
Bug : optimisation séquence renvoyait 413 "Request too large" car 1er fallback `llama-3.1-8b-instant` (TPM 6000) ne tient pas une requête d'optim (~7400 tokens). Correctif : ordre fallback inversé + déclenchement sur 413/503 (pas seulement 429).

## Risques
Aucun.
