# D05 — errorDialog modal global + niveau dans header

**Statut :** ✅ Livré 14-15/05/2026 — non commité
**Spec architecturale :** aucune (refacto UX — règle CLAUDE.md "erreurs en modal obligatoire")

## Fichiers concernés
- `frontend/src/errorDialog.js` *(new)* — module register/dispatch global (9 lignes)
- `frontend/src/App.jsx` — `registerErrorHandler(setAlertDialog)` au mount + remplacement `setErreur → setAlertDialog` + `useEffect` resync `params.niveau` ↔ `user.niveau` + prop `niveau` passée au Header
- `frontend/src/components/Header.jsx` — prop `niveau` + affichage `matière - niveau`
- `frontend/src/components/Ambiguites.jsx` — `showError` au lieu de `setErreur` inline
- `frontend/src/components/Optimiseur.jsx` — idem
- `frontend/src/components/SequenceForm.jsx` — idem

## Prochaine action
Commit dédié : `feat(ui): errorDialog modal global + matière-niveau dans header`

## Risques
Aucun — adoption règle CLAUDE.md "erreurs en modal", bug fix resync niveau.
