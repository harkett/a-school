# D16 — Consolidation du cœur (filet de tests + suspect sous filet)

**Statut :** 🎯 CHANTIER ACTIF · Phase 1 close (filet vert), Phase 2 en cours
**Origine :** récupère le contenu vivant de l'ex-`PLAN_REPRISE_ASCHOOL.md` (supprimé ; conservé dans l'historique git). Le plan de crise de mai est devenu ce chantier dans la boussole — un seul pilote.
**Verdicts détaillés des cases d'audit :** [TABLEAU DE BORD](../TABLEAU-DE-BORD.md) § AUDIT.

---

## Objectif

Consolider le cœur du produit, **valider ce qui est suspect tâche par tâche, sous filet de tests**, puis **rouvrir le push proprement**. Pas relancer l'ingénierie — sécuriser l'existant. Deepgram reste hors push.

---

## État des phases

### Phase 0 — Voir la réalité ✅ TERMINÉE (31/05)
Double piste : UI jugée par l'humain + API exercée en automatique (HTTP in-process, BDD mémoire, user fictif). **Résultat : le golden path TIENT au runtime, aucun symptôme bloquant.**
- Seul point relevé (non bloquant) : qualité de l'**optimiseur de séquence** « acceptable mais à améliorer » → parké en item qualité (BACKLOG), pas un bug.

### Phase 1 — Filet de tests sur le cœur ✅ TERMINÉE (31/05)
`test_endpoints_coeur.py` — Groq mocké (zéro réseau), BDD mémoire. Couvre les 5 endpoints centraux (`generate`, `generate-sequence`, `detect-ambiguites`, `analyser-consigne`, `optimize-sequence`) : happy path 200 + sortie cohérente, 401, 400/422, résilience Groq (fallback 429/413/503, 502, 429).
- Baseline réelle = **14 tests** (vérifié `git show HEAD`). P3.4 l'a portée à **17 verts**.

### Phase 2 — Traiter le suspect du cœur, sous filet, tâche par tâche 🔄 EN COURS
1. ✅ **2.1 — auto-save sans try/catch (perte silencieuse)** — FAIT (31/05) : helper `frontend/src/utils/activites.js` (lève sur échec réseau OU HTTP non-ok) + modal d'alerte dans `App.jsx`. Test `node --test` 4/4. Payload inchangé.
2. ✅ **Triage des 8 cases de l'audit Activité 15/05** — FAIT (31/05) : 4 à corriger (ordre **P3.4 → P3.6 → P5.11 → P3.5**), 2 sous-D (P4.7, P5.10), 2 garder/défer (P4.8, P4.9).
   - ✅ **P3.4 — FAIT (08/06)** : `/api/generate` ne passait pas par `call_groq` → except fourre-tout 500. Durci en `ValueError → 400` (clé inconnue) / `RuntimeError`+`RequestException` → 502 (Groq down). Happy path inchangé. +3 tests (14 → 17).
   - ⏳ **Prochain : P3.6**, puis **P5.11**, puis **P3.5**.

**Méthode par point :** reproduire → corriger → test vert → validé. Verdict par point (garder / corriger / abandonner).
**Critère de sortie :** plus de symptôme bloquant sur le golden path activité + séquence ; filet vert.

### Phase 3 — Geler proprement le build-ahead ⏳ À VENIR
- **RAG** : laisser en DEV / désactivé (`RAG_ENABLED=false`). Hébergement = décision reportée.
- **L37 / D07** : trancher le sort du code dormant **[DÉCISION]** — (a) commit WIP, (b) branche WIP, (c) suppression. Reco : ne pas câbler maintenant ; isoler ou étiqueter.
**Critère de sortie :** plus de code dormant ambigu dans le périmètre de reprise.

### Phase 4 — Rouvrir le push ⏳ À VENIR
Une fois le cœur validé + testé : re-vérifier le **nombre exact de commits d'avance** (a changé depuis le gel — Deepgram parti sur branche, dictée recâblée sur `main`), puis décider du franchissement, **Deepgram restant hors push**.
**[DÉCISION]** : tout valider en bloc puis push, ou isoler le sous-ensemble validé.
**Critère de sortie :** prod à jour avec uniquement du code validé + testé.

---

## Os restant (étiqueté)

| Os | Pourquoi enfoui | Rouvrir quand |
|---|---|---|
| **L37 / D07** (code dormant, non commité) | Tant qu'il dort, il ne nuit pas. Le câbler maintenant = travailler par-dessus du flou. | Quand on visera « Séquences 100% » ([D13](D13.md)) avec l'affinage. |

---

## Prochaine action

**P3.6** — corriger sous filet (reproduire → corriger → test vert → validé), puis **P5.11**, puis **P3.5**. Verdicts : [TABLEAU DE BORD](../TABLEAU-DE-BORD.md) § AUDIT.
