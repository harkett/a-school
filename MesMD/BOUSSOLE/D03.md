# D03 — INFRA-RAG branchée DEV

**Statut :** ✅ Livré 15/05/2026 — non commité · DEV uniquement, pas en prod
**Spec architecturale :** [INFRA-RAG.md](../RAG/INFRA-RAG.md)

## Fichiers concernés
- `backend/rag/` *(new)* — `client.py`, `embeddings.py`, `retrieve.py`, `_canary_inject.py`, `_test_retrieve.py`, `chroma_db/`
- `backend/routers/generate.py` — gates RAG (`RAG_ENABLED` + matière maths + niveau cycle 4) + fallback silencieux + logs INFO
- `src/prompts.py` — `_build_rag_prefix()` injecté en tête du prompt
- `backend/models.py` — `GenerateResponse.chunks` populé quand RAG actif
- `rag_demo/` *(new, legacy)* — POC à supprimer après commit

## Prochaine action
1. Commit dédié : `feat(rag): infrastructure RAG mutualisée + branchement /api/generate (DEV)`
2. Supprimer `rag_demo/` (migré dans `backend/rag/`)
3. Hébergement `chroma_db/` prod + pré-chargement modèle au boot (lifespan FastAPI)

## Risques
Aucun en l'état — `RAG_ENABLED=false` par défaut, gates protègent. Test canary Z36-27 validé 15/05.
