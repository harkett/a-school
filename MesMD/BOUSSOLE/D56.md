# D56 — BTS CIEL Option A : artefact pré-réforme à réconcilier AVANT de traiter le cycle BTS (risque préventif)

> Item TRACKER : **56**. Constat posé le 03/07/2026 (parenthèse pendant le chantier miroir P0→P6). Tâche **non démarrée** — tracée pour ne pas se faire surprendre quand on traitera le cycle BTS avec la nouvelle procédure. Aucune décision prise ici.

## Pourquoi cette fiche

BTS CIEL Option A a été traité **AVANT les réformes**, à l'ancienne méthode (référentiel déposé + fiche Python de réglages + chunks). Risque : **conflit** quand on traitera le cycle BTS avec la **nouvelle procédure data-driven**. Faits vérifiés en lecture seule (base + code + mémoire) le 03/07/2026.

## État réel — BASE (`aschool`)

- **Référentiel** : `referentiels` **id=1** — niveau « BTS CIEL Option A » (niveau_id **42**, cycle BTS), `matiere_id` NULL (tout le niveau), `nom_fixe` `bts_ciel_option_a`, `fichier` `referentiel.pdf`, source « dépôt manuel », **`traite = f`**.
- **Chunks** : **236** lignes dans `referentiel_chunks` pour `referentiel_id=1` (colonnes : id, referentiel_id, chunk_index, option_ab, page, texte, embedding, embedding_model, created_at).
- **Cycle BTS = 17 niveaux** (des spécialités), **tous `traite=f`** : SIO, Commerce International, NDRC, MCO, CG, SAM, Électrotechnique, Génie Civil, Bâtiment, Professions Immobilières, Banque, Assurance, Tourisme, Audiovisuel, Design, **CIEL Option A (42)**, **CIEL Option B (43)**.
- **Matières : AUCUNE reliée à un niveau BTS** — CIEL Option A (42) n'a **zéro** paire `matiere_niveaux`.

## État réel — CODE

- `backend/rag/referentiels/bts_ciel_option_a.py` : « **PREMIÈRE fiche, PAS un gabarit universel** » (réglages propres à CIEL — `PDF_PATH`, découpe `MAX_CHARS`/`OVERLAP`, tag option A/B via `OPTION_B_SECTIONS = {II.2.3, III.1.3, III.2.2}`, `SCORE_MIN=0.33`). Échafaudage **transitoire** assumé.
- Constante `NIVEAU = "BTS CIEL option A"` (**« option » minuscule**) posée à l'ingestion — **≠** base `niveaux.nom = "BTS CIEL Option A"` (**majuscule**). Mismatch de casse.
- « CIEL » / `bts_ciel` cité dans **10 fichiers backend** : `referentiels_admin.py`, `exemple_referentiel.py`, `admin.py`, `rag/referentiels/bts_ciel_option_a.py`, `models_db.py`, `programmes.py`, `rag/pgvector_store.py`, `rag/referentiels/__init__.py`, `activites.py`, `transcribe.py`.

## État réel — MÉMOIRE (notes, dont périmées)

- « BTS CIEL + ses **9 matières** » (fiches `project_matieres_hybride_socle_referentiel`, `feedback_jamais_valeur_fausse`, `feedback_tests_manuels_reels`) → **contredit par la base** : 0 matière reliée à CIEL aujourd'hui.
- `feedback_nom_bts_ciel_complet` : « niveau id **26** » → base dit id **42**. Périmé.

## Les conflits à traiter (préventif)

1. **Matières fantômes.** Le seed `seed_bts_ciel.py` (qui posait les 9 matières CIEL) a été **supprimé** (commit `70f7aa1`) ; elles ne sont **plus en base**. Doc/mémoire les décrivent pourtant comme faites. À réconcilier : soit on les réinjecte par la nouvelle voie (extraction référentiel), soit on acte qu'elles sont à refaire.
2. **Fiche Python en dur par référentiel.** `bts_ciel_option_a.py` = exactement le « code par cas » que la doctrine `couple_data_driven` veut supprimer. Traiter les autres BTS avec la nouvelle procédure impose de décider : garde-t-on une fiche Python par spécialité, ou rend-on ça data-driven ?
3. **Casse du niveau.** « option » (code) vs « Option » (base) → tout appariement par chaîne exacte échoue silencieusement.
4. **`traite=f` malgré référentiel + 236 chunks.** Le couple n'est pas marqué « traité » alors que les données existent → incohérence de drapeau à clarifier.

## Portée / limites

- **Ne rien décider maintenant** : cette fiche **constate** et liste les conflits. Les choix (réinjecter les matières ? supprimer/garder la fiche Python ? aligner la casse ?) se tranchent le jour où on attaque le cycle BTS.
- Recoupe : ligne « Extraire les matières des BTS » (§ Refonte programmes / annexe A7), item **55** (socle cycles 7-11), doctrine `couple_data_driven`, `project_matieres_hybride_socle_referentiel`.
